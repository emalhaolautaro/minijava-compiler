///[SinErrores]
// Prueba la declaracion simple de una interfaz con un metodo abstracto.

interface MiInterfaz {
    // Las interfaces definen metodos sin cuerpo (abstractos por defecto).
    void unMetodo(int a);
}