///[SinErrores]
/* Comentario en bloque de apertura */
(a + b) * c / d - e % f;
g++;
h--;
if (x == y && z != w || a >= b && c <= d) {
    arr'0' = obj.metodo(x, y);
    resultado = (x < y) | x : y;
}
{
    // Otro bloque
    x = y;
}
x = y > z;
y = x < z;
z = !x;
a = b != c;
b = c == d;
c = d >= e;
d = e <= f;
e = f && g;
f = g || h;
g = h % i;
h = i + j;
i = j - k;
j = k * l;
k = l / m;
l++;
m--;
/* Comentario en bloque de cierre */